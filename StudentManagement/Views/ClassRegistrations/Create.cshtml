@model StudentManagement.Models.ClassRegistration
@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Class Registration</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="StudentId" class="control-label"></label>
                <select asp-for="StudentId" asp-items="ViewBag.StudentsList" class="form-control"></select>
                <span asp-validation-for="StudentId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ClassId" class="control-label"></label>
                <select asp-for="ClassId" asp-items="ViewBag.ClassesList" class="form-control"></select>
                <span asp-validation-for="ClassId" class="text-danger"></span>
            </div>
            <br />
            <div class="form-group">
                <button type="submit" value="save" class="btn btn-primary">Save <i class="fa-regular fa-floppy-disk"></i></button>
                <a asp-action="Index" class="btn btn-secondary">Back <i class="fa-solid fa-angles-left"></i></a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
